{
  "name": "identity-contract",
  "$schema": "../../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "frontend/libs/modules/identity/contract/src",
  "prefix": "erp",
  "projectType": "library",
  "tags": ["scope:identity", "type:contract"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../../../../../coverage/frontend/libs/modules/identity/contract"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
