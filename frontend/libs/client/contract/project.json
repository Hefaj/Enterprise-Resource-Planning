{
  "name": "contract",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "frontend/libs/client/contract/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["scope:host", "type:contract"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../../../../coverage/frontend/libs/client/contract"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
