<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <AnalysisLevel>latest-recommended</AnalysisLevel>
    <EnforceCodeStyleInBuild>true</EnforceCodeStyleInBuild>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../Catalog.Domain/Catalog.Domain.csproj" />
    <ProjectReference Include="../../../building-blocks/Erp.BuildingBlocks.Application/Erp.BuildingBlocks.Application.csproj" />
    <!--
      Referencja do BuildingBlocks.Api jest tu WYŁĄCZNIE dla typów kontraktu żądań
      (PagedRequest, SearchResponse, TreeSelectionRequest) — są częścią kontraktu API,
      którego kształt jest zamrożony ze względu na wygenerowane klienty NSwag.
      Nie wolno stąd sięgać po nic z ASP.NET ani FastEndpoints.
    -->
    <ProjectReference Include="../../../building-blocks/Erp.BuildingBlocks.Api/Erp.BuildingBlocks.Api.csproj" />
  </ItemGroup>

</Project>
