import { AutomationRuleDto } from '../../api-client';

/** Reguła automatyzacji w widoku — bez wzbogacenia, `AutomationRuleDto` niesie już wszystko
 * potrzebne (AUT-001). */
export type AutomationRuleVM = AutomationRuleDto;
