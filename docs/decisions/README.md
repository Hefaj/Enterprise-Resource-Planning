# Decyzje architektoniczne

Ten katalog przechowuje przyszłe ADR-y dla decyzji, których uzasadnienia nie da się czytelnie
utrzymać w głównym dokumencie architektonicznym. ADR zawiera kontekst, decyzję, konsekwencje i status;
nie jest planem implementacji.
